# AI-WAREHOUSE v4

# ── 敏感文件/凭据（每行一条规则）──
*.key
# MES 只读库连接凭据 + 本地 agent 覆盖配置（真实值不入库；模板 db.local.example.json 入库）
agent/mes-implement-expert/config/db.local.json
agent/mes-implement-expert/config/agent.local.yaml
*.pem
*.p12
*.pfx
*.cert
*.crt
*.csr
kubeconfig*
!kubeconfig.example
.env
.env.*
!.env.example

# ── 基础设施 / 云 ──
.terraform/
*.tfstate
*.tfvars
!*.example.tfvars
*vault*
*.vault

# ── 日志 / 编辑器 / 系统 ──
*.log
/var/log/
logs/
.DS_Store
Thumbs.db
*.swp
*~
.vscode/
.idea/
*.iml

# ── 临时 / 备份 ──
tmp/
temp/
*.bak
*.old

# ── 大文件 / 模型权重 ──
*.iso
*.dmg
*.bin
model-weights/
*.pt
*.pth
*.onnx
*.gguf

# ── Docker 数据卷 ──
**/docker/data/
**/docker/volumes/

# 客户原始资料不入库（大文件+敏感数据，仅存本地 inbox/ 与项目 input/）
# 仓库只沉淀整理成果(MD 文档)，原件保留在本地
*.xlsx
*.xls
*.pptx
*.ppt
*.jpeg
*.jpg
*.png
*.gif
*.bmp
*.tiff
*.doc
*.docx
*.pdf
# 例外：smes-v61-training/flows/ 精选流程图资产（每模块 5-8 张代表图，随 md 入库供汇报回溯；全量图仍留在本地 raw/flows-all/ 不入库）
!agent/mes-implement-expert/data/smes-v61-training/flows/**/*.png
# 客户原始资料不入库（仅存本地 inbox/，仓库只沉淀整理成果 MD；README.md 除外）
# 覆盖飞书多维表格 .base 原件、SQL 查询脚本、html 大体积文档等
delivery/inbox/*
# 各归属目录的 raw/ 原件区：客户原始文件（html/pptx/sql 等）只存本地，不入 git
**/raw/
!delivery/inbox/README.md
# 日常工作数据输入窗口：会议纪要等敏感原始材料不入库，仅放行目录说明 README
!delivery/inbox/日常工作数据输入窗口/
delivery/inbox/日常工作数据输入窗口/*
!delivery/inbox/日常工作数据输入窗口/README.md
# 日常运维报错截图：报错截图原始图片不入库，仅放行目录说明 README
!delivery/inbox/日常运维报错截图/
delivery/inbox/日常运维报错截图/*
!delivery/inbox/日常运维报错截图/README.md

# ── worklog 日志与附件入库（覆盖上面的 logs/ 与 *.docx 全局规则）──
# 工作日志是权威数据源，必须 git 可追溯；附件（聊天记录等）随记录入库
!agent/mes-report-agent/data/worklog/logs/
!agent/mes-report-agent/data/worklog/logs/*
!agent/mes-report-agent/data/worklog/attachments/
!agent/mes-report-agent/data/worklog/attachments/*
