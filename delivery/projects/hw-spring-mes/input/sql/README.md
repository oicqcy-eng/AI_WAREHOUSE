# sql — 数据/查询脚本

项目相关的 SQL 脚本与数据模板。

## 存放内容

- 项目现场的数据查询脚本
- 数据导出/清洗脚本
- 报表取数 SQL 模板

## 规范（重要）

1. **脱敏**: 表名/字段若含客户真实命名，替换为通用命名并标注映射
2. **不存真实数据**: 查询结果/数据集不落地入库（客户数据留在客户侧），只存**查询逻辑**
3. **只读**: 一律 `SELECT`，含 `LIMIT` 防大表扫描
4. 通用查询模板沉淀到 `agent/mes-implement-expert/tools/query-templates.sql`
