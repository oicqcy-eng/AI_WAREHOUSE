# _shared — 跨 Agent 公共资产

存放跨多个 Agent 复用的通用资源，**不得混入业务专属内容**。

建议内容：

- 通用 Prompt 模板
- 通用工具脚本（Excel 处理、文档导出等）
- 模板与 Logo 等品牌资产
- 公共配置片段

## 边界

- 仅放「2 个及以上 Agent 都会用」的资产
- 单个 Agent 专属内容 → 放对应 Agent 自己的目录
- 基础设施级资产（数据库、网关、监控）→ `shared/`
